name: Backend CI/CD

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'backend/**'

jobs:
  test:
    name: Test Backend
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v4
    
    - name: Configurar Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json
    
    - name: Instalar dependências
      working-directory: ./backend
      run: npm ci
    
    - name: Verificar código (lint)
      working-directory: ./backend
      run: npm run lint || echo "Lint não configurado ainda"
    
    - name: Build do projeto
      working-directory: ./backend
      run: npm run build
      env:
        NODE_ENV: production
    
    - name: Executar testes
      working-directory: ./backend
      run: npm test || echo "Testes não configurados ainda"

  build:
    name: Build Backend
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v4
    
    - name: Configurar Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json
    
    - name: Instalar dependências
      working-directory: ./backend
      run: npm ci
    
    - name: Build do projeto
      working-directory: ./backend
      run: npm run build
      env:
        NODE_ENV: production
    
    - name: Upload do artefato de build
      uses: actions/upload-artifact@v4
      with:
        name: backend-build
        path: backend/build/
        retention-days: 7

  # Descomente e configure para deploy automático
  # deploy:
  #   name: Deploy Backend
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   
  #   steps:
  #   - name: Checkout código
  #     uses: actions/checkout@v4
  #   
  #   - name: Deploy para Strapi Cloud
  #     run: |
  #       echo "Configure aqui o deploy para seu servidor"
  #       # Exemplo: deploy para Strapi Cloud, Railway, Heroku, etc.

